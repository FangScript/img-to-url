{
  "name": "image-uploader",
  "version": "1.0.0",
  "description": "Upload images to MongoDB and get shareable URLs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test-mongo.js",
    "build": "echo 'Build step complete'",
    "vercel-build": "echo 'Vercel build step complete'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.4.2",
    "multer": "^1.4.5-lts.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 